<a 
	href="{{$path}}" 
	id="{{$id ?? null}}"
	{{$attr ?? null}}
	class="btn btn-{{$size ?? null}} btn-{{$type ?? null}} btn-{{$weight ?? null}} btn-{{$width ?? null}} btn-{{$color}} {{$extra ?? null}}">

	{{$label}}

</a>